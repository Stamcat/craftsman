export type Theme = Record<string, string>;

export const themes: Record<string, Theme> = {
    default: {
        "--blue600": "#2563eb",
    },
    green: {
        "--blue600": "#16a34a",
    },
};
