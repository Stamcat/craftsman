# Global Styles
- components > button is declared in componentSelectors, I'd prefer it to rely on index to build out the list of components available for theming
- GlobalStyles object declared in preview.tsx should be something we provide as a global component
- need a story explaining how to set up themes
- typing on the themes object needs to be refined, currently just Record`<string, Theme>`, the string should be typed
